{% extends 'main/main.html.twig' %}
{% block additionalcss %}
    <link rel="stylesheet" href="{{ asset('css/add.css') }}" />
{% endblock %}
{% block content %}
    <div class="content" id="articles">
        <h3>{{ 'add_article' | trans }}</h3>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">{{ 'add_article' | trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}